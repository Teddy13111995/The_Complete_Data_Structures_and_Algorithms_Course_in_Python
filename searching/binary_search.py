# Binary Search
# Faster than linear search
# half of remaining element is eliminated at a time
# only works on sorted arrays
# def binary_search(l,key):
#     sorted_array = l.sort()
#     n = len(sorted_array)
#     if (key == sorted_array[n//2]):
#         print(f"key found at{n//2}")
#     elif (key<sorted_array):
#         new_list = sorted_array(0:n//2)
#
#
